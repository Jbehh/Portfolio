{
  "name": "fresh-grad-portfolio-tailwind-v4",
  "version": "1.0.0",
  "private": true,
  "description": "Professional fresh graduate portfolio using Tailwind CSS v4.1 (CLI) with modular JS animations.",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./src/input.css -o ./public/output.css --watch",
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./public/output.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.14",
    "tailwindcss": "^4.1.14"
  }
}
